const request = require("supertest"); // Load SuperTest Library
const pastes = require("../src/data/pastes-data"); // Load pastes data
const app = require("..src/app"); // Load Express server

describe("path /pastes", () => {
    // Add tests here
})